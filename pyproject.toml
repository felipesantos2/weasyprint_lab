[project]
name = "weasyprint-lab"
version = "0.1.0"
description = "Uma pequena demonstração sobre o uso do WeasyPrint junto ao Jinja para rederizar PDFs. Junto a um estudo sobre testes"
readme = "README.md"
authors = [
    { name = "felipesantos2",  email = "santospinheiro627@gmail.com" }
]
requires-python = ">=3.13"

dependencies = [
    "boto3>=1.43.83",
    "flask>=3.1.3",
    "google-cloud-storage>=3.13.1",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.3",
    "rich-cli>=1.8.1",
    "weasyprint>=69.0",
]

[project.urls]
Repository =  "https://github.com/felipesantos2/weasyprint_lab"

# [project.scripts]
# cli = "weasyprint_lab.cli:run"

# [build-system]
# requires = ["uv_build>=0.12.5,<0.13.0"]
# build-backend = "uv_build"

[tool.uv]
package = false


[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.3",
    "ty>=0.0.72",
]

[tool.ruff]
line-length=88
fix=true
show-fixes=true

[tool.ty]
